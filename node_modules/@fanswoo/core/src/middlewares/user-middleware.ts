import 'bootstrap';
import { AjaxSubmit } from '@fanswoo/ajax-submit/main';
import { Middleware } from '@fanswoo/controller/main';

export default class UserMiddleware extends Middleware {
  public handle(): void {
    const ajaxSubmit = new AjaxSubmit();
    ajaxSubmit.bindEvent();
  }
}
