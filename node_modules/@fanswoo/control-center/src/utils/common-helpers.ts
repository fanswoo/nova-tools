const setSidebarOpenedCookie = (opened: boolean) => {
  try {
    axios.post('api/admin/set-sidebar-opened-cookie', {
      opened,
    });
  } catch (error) {
    console.error(error);
  }
};

export default { setSidebarOpenedCookie };
export { setSidebarOpenedCookie };
