import 'reflect-metadata';
import { Container } from 'inversify';
// import ISideBar from '@fanswoo/core/vue/ControlCenter/Contracts/Layouts/SideBar/SideBar';
// import SideBar from '@fanswoo/core/vue/ControlCenter/Layouts/SideBar/SideBar.vue';

const ControlCenterProvider = new Container({
  // skipBaseClassChecks: true
});

// ControlCenterProvider.bind<ISideBar>(Symbol.for('SideBar')).to(SideBar);

export default ControlCenterProvider;
