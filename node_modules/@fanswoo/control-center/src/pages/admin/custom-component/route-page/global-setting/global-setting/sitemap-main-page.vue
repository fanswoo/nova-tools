<template>
  <div class="sitemap-main-page">
    <PageNavigation :title="'全域設定'" :subtitle="'功能導覽'" />
    <div class="card-group-content">
      <template v-for="(group, key) in cardGroups":key="key">
        <div class="card-group">
          <h4>{{ group.title }}</h4>
          <b-card-group columns>
            <template v-for="(card, key2) in group.cards" :key="key2">
              <b-card border-variant="light" :header="card.title">
                <template v-for="(item, key3) in card.items" :key="key3">
                  <b-card-text v-if="item.title && item.linkTo">
                    <router-link :to="item.linkTo">
                      {{ item.title }}
                    </router-link>
                  </b-card-text>
                </template>
              </b-card>
            </template>
          </b-card-group>
        </div>
      </template>
    </div>
  </div>
</template>

<script>
import CustomRoutePage from '@fanswoo/control-center/core/custom-route-page.vue';
import PageNavigation from '@fanswoo/control-center/layouts/page-navigation.vue';

export default {
  props: {
    cardGroups: {
      type: Array,
      default: function () {
        return [];
      },
    },
  },
  mixins: [CustomRoutePage],
  activated: function () {
    this.$emit('initPageData');
  },
  components: {
    PageNavigation,
  },
  data: () => {
    return {};
  },
  methods: {},
  computed: {},
  mounted() {},
};
</script>

<style lang="scss" scoped>
@import '@fanswoo/core/style/modules/boot.scss';

.card-group-content {
  padding-top: 10px;
  @include media-down(sm) {
    padding-top: 60px;
  }
  .card-group {
    margin: 0 0 10px;
    background: #fff;
    padding: 20px;
    h4 {
      margin-bottom: 1rem;
    }
    .card-columns {
      column-count: 6;
      @include media-down(lg) {
        column-count: 5;
      }
      @include media-down(md) {
        column-count: 4;
      }
      @include media-down(sm) {
        column-count: 3;
      }
      @include media-down(sm) {
        column-count: 1;
      }
    }
  }
}
</style>
