<template>
  <div class="line-content" lineContent="productNames" v-cloak>
    <div class="title">產品組合</div>
    <div class="field">
      <textarea
        v-if="requestId"
        disabled="true"
        v-model="productNames"
      ></textarea>
      <textarea
        v-else
        id="product-names"
        name="productNames"
        v-model="productNames"
        placeholder="A系列
            B系列"
      ></textarea>
      <div class="explanation">
        一個銷售組合應先區分為多個產品系列，請填寫產品系列名稱，每個系列名稱一行，例如：「A系列、B系列」
      </div>
    </div>
  </div>
</template>

<script>
import EventBus from '@fanswoo/core/bootstrap/event-bus';

export default {
  props: ['requestId'],
  data: () => {
    return {
      productNames: '',
    };
  },
  methods: {},
  watch: {
    productNames(value) {
      EventBus.$emit('changeProductNames', value);
    },
  },
  mounted() {
    EventBus.$on('initProductNames', (productNames) => {
      this.productNames = productNames;
    });
  },
  beforeDestroy() {
    EventBus.$off();
  },
};
</script>
