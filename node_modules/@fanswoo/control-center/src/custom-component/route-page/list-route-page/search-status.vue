<template>
  <div class="search-status" v-if="searching" v-cloak>資料搜尋中 ...</div>
  <div class="search-status" v-else-if="ORMs.length === 0" v-cloak>
    這個條件沒有搜尋結果，請選擇其它篩選條件
  </div>
  <div
    class="search-status get-more"
    v-else-if="canSearchMore === 'yes'"
    v-cloak
    @click="onORMCollectionPush"
  >
    點選這裡或滾動到底部可以讀取更多資料 ...
  </div>
  <div class="search-status" v-else-if="canSearchMore === 'no'" v-cloak>
    資料已經全部搜尋完畢，沒有更多資料了
  </div>
</template>

<script>
export default {
  props: {
    ORMs: {
      type: Array,
      default: function () {
        return [];
      },
    },
    searching: {
      type: Boolean,
      default: false,
    },
    canSearchMore: {
      type: String,
      default: '',
    },
  },
  methods: {
    onORMCollectionPush() {
      this.$emit('onORMCollectionPush');
    },
  },
};
</script>
