<template>
  <div>
    <template v-for="(button, key) in buttons" :key="key">
      <router-link
        :to="{
          path: button.linkTo,
          query: button.withSearchSettingFields
            ? searchSettingFieldValues
            : null,
        }"
        :class="['btn', button.buttonStyle]"
        :target="button.targetBlank ? '_blank' : null"
      >
        <font-awesome-icon
          v-if="button.fontAwesomeIcon"
          class="font-awesome-icon"
          :icon="button.fontAwesomeIcon"
        />
        {{ button.text }}
      </router-link>
    </template>
  </div>
</template>

<script>
import FixedButtons from '@fanswoo/control-center/layouts/fixed-button/fixed-buttons.vue';

export default {
  props: {
    buttons: {
      type: [Array, Object],
      default: function () {
        return {};
      },
    },
    searchSettingFieldValues: {
      type: Object,
      default: function () {
        return {};
      },
    },
  },
  components: {
    FixedButtons,
  },
  methods: {
    onSettingButtonClick(event) {
      this.$emit('onSettingButtonClick', event);
    },
  },
};
</script>
