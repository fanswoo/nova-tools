"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const child_process_1 = require("child_process");
class PackageUpdator {
    constructor(config) {
        this.config = config;
        this.config = config;
    }
    update() {
        const { packages } = this.config.platforms.find((item) => item.name === 'npm');
        const packagesString = packages
            .map((item) => {
            return item.name;
        })
            .join(' ');
        packages.forEach((item) => {
            (0, child_process_1.execSync)(`npm update ${packagesString}`);
        });
        return true;
    }
}
exports.default = PackageUpdator;
//# sourceMappingURL=package-updator.js.map